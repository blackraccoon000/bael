import {signIn, signOut, useSession} from "next-auth/react";

const SigninPage = () => {
  // const [sessin, loading] = useSession();
  return (
    <div>
      <h1>Signin</h1>
    </div>
  );
};

export default SigninPage;
