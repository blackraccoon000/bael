// 請求情報
model Invoice {
  id        Int      @id @default(autoincrement()) // 請求ID
  works Work[]
  payment   Int
  received  Boolean @default(false)
  disabled  Boolean @default(false)
  createdAt DateTime @default(now())
  updateAt DateTime @default(now())
}